#include <stdio.h>

/**
 * prints out the phrase with proper grammar,
 * but the outcome is a piece of art,
 * Returns 0 if code  exts properly
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
